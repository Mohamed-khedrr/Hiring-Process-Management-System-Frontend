// Define the AssessmentStatus enum based on the Java enum values
export enum AssessmentStatus {
    Delivered = 'Delivered',
    Visited = 'Visited',
    Completed = 'Completed',
    Missed = 'Missed'
}
